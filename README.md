# dotfiles
dotfiles under your GitHub user, containing your .zshrc (aliases, oh-my-zsh config, etc.) and an install.sh script. GitHub Codespaces will automatically install your dotfiles into every new codespace you create once you enable it in settings. 
